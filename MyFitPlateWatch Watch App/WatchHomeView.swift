import SwiftUI

struct WatchHomeView: View {
//    @ObservedObject var sessionManager = WatchSessionManager.shared

    var body: some View {
        VStack(spacing: 8) {
           
        }
    }
}
